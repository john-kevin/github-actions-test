name: Shell Commands

on: [push]

jobs: 
    run-shell-command:
      runs-on: ubuntu-latest
      steps: 
        - name: echo a string
          run: echoc "Hello"
        - name: multiline script
          run: |
            node -v
            npm -v
        - name: install docker
          run: |
             sudo apt-get update -y 